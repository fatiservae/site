var PH = 7.40;
var PCO2 = 40;
var fr = 14;
function calcula(fr) {
    var pulmao = {
        fr: 14,
        vq: 8,
        volumeTotal: 3600,
        volumeCorrente: 600
    };
    var sangue = {
        ph: PH,
        pco2: PCO2
    };
    var paciente = {
        nome: "jose",
        pulmao: pulmao,
        sangue: sangue
    };
    fr = fr + 4;
    // Retornar um objeto com a propriedade fr
    return { fr: fr };
}
//setInterval(run, 1000);
